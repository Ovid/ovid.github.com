[%# Site-wide notice. Rendered inside .prose but outside <article>, so it is
    page chrome rather than article content. wrapper.tt runs only `content`
    through the blogdown filter, so this is HTML rather than Markdown. %]
<aside class="global-message">
  <p>Read <a href="/paad/">the new tutorial on writing <em>production-quality</em> software with AI</a>.</p>
</aside>
